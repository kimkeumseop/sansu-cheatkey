# 개운산 애드센스 개선 기록

## 2026-09-23 조사

- 애드센스는 `gaeunsan.vercel.app`을 `주의 필요`, `가치가 별로 없는 콘텐츠`로 표시한다. `ads.txt`는 외부 HTTP에서 정상 제공되지만 애드센스 화면은 `찾을 수 없음`으로 표시한다.
- 서치콘솔은 8개 색인, 40개 미색인이다. 38개가 `발견됨 - 현재 색인이 생성되지 않음`이다.
- 산 30개 페이지가 짧은 공통 틀을 반복하고, 추천 점수를 실측 확률처럼 `%`로 표시한다. 일부 문구는 검증되지 않은 건강·운세 효과를 사실처럼 단언한다.
- 원본 `D:/명산/sansu-cheatkey`에는 사용자 또는 다른 작업의 미완료 변경이 있다. 이 변경은 보존한다. 개선 작업은 `D:/명산/sansu-cheatkey-adsense` 독립 워크트리에서 진행한다.
- 목표는 승인 보장이 아니라 독자가 실제로 쓸 수 있는 정확한 정보, 명확한 편집 기준, 중복 축소, 검증 가능한 배포다.

## 2026-09-23 변경 및 검증

- 30개 개별 산 소개는 기능상 유지하되 검색 색인을 막고 사이트맵에서 제외했다. 지역·예시 코스 시간·교통·장소 이름을 한곳에서 비교하는 `/sansu/mountains`를 추가했다.
- 출생 연도만 사용하는 기능을 완전한 사주 풀이처럼 표현하지 않도록 홈·폼·결과·공유·메타데이터를 고쳤다. 사용자에게 표시되던 퍼센트·별점은 내부 정렬 점수의 의미를 왜곡하므로 제거했다.
- 의학적 효과를 단정하던 글을 안전한 산행 계획 글로 다시 썼고, 오성체·띠 글은 사이트의 실제 분류표와 한계를 설명하도록 다시 썼다. 검증되지 않은 개별 산 이야기와 유행 메모, 장소 설명은 확인 전까지 화면에서 숨겼다.
- `ads.txt`는 기존 파일을 유지했다. AdSense 사이트 인증용 `google-adsense-account` 메타태그와 Search Console 소유권 확인 태그도 유지했다. 광고 스크립트는 실제 광고 승인 전 페이지에서 제거했다. 공식 도움말에 따르면 AdSense는 메타태그로 사이트 소유권을 검증할 수 있다.
- `npm run lint` 오류 0건, `npm run build` 성공. 로컬 HTTP 확인 결과 홈·비교·소개 200, 산 상세 `noindex, follow`, 사이트맵 18개 URL, `ads.txt` 200 및 퍼블리셔 ID 정상이다.
- 원본 작업 폴더의 미완료 변경은 전혀 수정하지 않았다. 배포와 서치콘솔·애드센스 재확인은 아직 남았다.

## 2026-09-23 배포 확인과 마지막 내용 교정

- 커밋 `bf1c598`을 원격 `main`에 빠르게 병합 가능한 상태로 반영했고 Vercel 배포 성공 및 공개 URL HTTP 응답을 확인했다. 산 비교·소개·수정 글이 200, 산 상세는 `noindex`, 사이트맵은 18개 URL, `ads.txt`는 200이다.
- 공개 비교표 검토 중 강화 보문사의 옛 선착장 안내와 통도사역 표기를 발견했다. 강화군과 통도사의 현재 공식 방문 안내를 확인해 교통 메모를 교정한다.
- 사용자가 서치콘솔 사이트맵 제출과 애드센스 재검토 요청을 모두 승인했다. 최종 교정 배포 확인 후 두 요청을 진행한다.

## 2026-09-23 제출 완료

- 최종 교정 커밋 `ae5bf4d`의 Vercel 배포가 성공했고, 운영 산 비교 페이지에서 교정된 교통 메모가 보이는 것을 확인했다.
- Search Console의 `https://gaeunsan.vercel.app/` 속성에 `/sitemap.xml`을 다시 제출했다. 화면에 `사이트맵이 제출됨`, 제출일 `2026. 9. 23.`이 표시됐다. 마지막 읽은 날짜와 발견 페이지 수는 아직 이전 값이므로 Google의 재처리를 기다려야 한다.
- AdSense에서 사이트 소유권 확인 방식을 공개 HTML에 있는 메타 태그로 선택하고, 개운산의 문제 수정 확인 후 검토 요청을 제출했다. 화면에 `준비 중`, `리뷰가 요청됨`이 표시됐다.
- 승인 여부는 Google 심사에 달려 있다. Ads.txt는 공개 URL에서 HTTP 200이며 퍼블리셔 ID가 일치하지만 AdSense 표에는 아직 `찾을 수 없음`으로 남아 있다.
